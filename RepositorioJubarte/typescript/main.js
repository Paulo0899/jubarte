var minhaVar = 'minha variavel';
function minhaFuncao(x, y) {
    return x + y;
}
var valor = [1, 2, 3];
valor.map(function (valor) {
    return valor * 2;
});
valor.map(function (numeros) {
    numeros * 2;
});
